#include <stdio.h>
#include <string.h>

// to print to a text file the payload to be read for DBG programs
// ./input > abc

 int main() {
  char buf[13] = ""; // insert payload here

  printf("%s\n", buf);
}
